services:
  - type: web
    name: digital-twin-risk-demo
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn web_app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0rc1
      - key: OPENAI_API_KEY
        sync: false
